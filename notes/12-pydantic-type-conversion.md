# Pydantic Type Conversion

Pydantic validates incoming data using Python type hints.

Required fields must be present.

Optional fields can use default values.

Even optional fields are validated when provided.

Key Learning:
Optional does not mean unvalidated.